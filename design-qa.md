# T0～T4 主题视觉 QA

## 对照范围

- 晶海星港参考：`docs/diy/ui/02-晶海星港-现有布局约束视觉稿.png`
- 星穹田园公社参考：`docs/diy/ui/03-星穹田园公社-现有布局约束视觉稿.png`
- 角色参考：`docs/diy/ui/04-晶海星港-角色视觉系统.png`、`05-星穹田园公社-角色视觉系统.png`
- 真实 Electron：`.work/runtime/t0-t4/starship-1586x992-final.png`、`farm-1586x992-final.png`
- 精细碰撞运行复核：`.work/runtime/t0-t4/crystal-fine-collision-runtime-after-12s.png`、`farm-fine-collision-runtime-after-12s.png`
- 同屏对照：`.work/runtime/t0-t4/qa-crystal-final-comparison.png`、`qa-farm-final-comparison.png`
- 碰撞覆盖：`.work/runtime/t0-t4/crystal-collision-overlay-v3.png`、`farm-collision-overlay-v3.png`
- 视口：目标稿为 1586×992；Electron 在同一逻辑视口捕获，Retina 原图为 3172×1984，同屏对照归一到 1586×992。

## 结论

高保真返工已把两套主题从“程序化暗色覆盖”收敛为完整地图和角色资产。地图的地板、边界、工位、会议区、远景、建筑和主题道具均与目标稿同一语汇；两套 15 角色头像和场景 Sprite 已进入真实运行，且修复了不等宽三视图被机械切片造成的半身问题。

| 级别 | 核验项 | 结果 |
| --- | --- | --- |
| P0 | 主题切换不得停止或重建 Agent、PTY、Session、任务、记忆和工作区 | 通过；同一 Harness、同一组 6 Agent 往返，既有无损状态哈希保持 |
| P0 | 设置、右侧 Command Center、底部 Agent 条的入口、顺序与尺寸心智保持 | 通过；真实截图结构一致，仍在设置卡片中一步切换 |
| P1 | 两套地图达到目标稿的地板、建筑边界、工位、主要道具和远景辨识度 | 通过；真实整图资产与目标稿同屏对照，无程序化默认办公室残留主导画面 |
| P1 | 每套主题覆盖 15 个稳定角色槽位，头像与地图角色一致且身体完整 | 通过；30 个头像、630 个非空场景帧；90 个基础方向帧头脚完整、不触碰切片左右边界 |
| P1 | 人物识别桌椅、房间、池塘、道路，角色之间不互穿 | 通过；命名精细碰撞体按 3.5 px 净空烘焙两张 34×22 导航投影，全部 23 个操作点从入口可达且相邻可走边无中途擦碰；真实 6 Agent 在双主题切换后持续移动 12 秒，人物保持在桌、墙、房屋、池塘和设备轮廓外 |
| P1 | 中文主题入口、当前态、失败提示和一步切换可读 | 通过；三主题卡片和先校验后持久化合同保持 |
| P2 | Shell 的表面、边框、选中态随主题变化，但不改变字段与操作路径 | 通过；仅有限 Token 覆盖既有表面 |
| P2 | 浅色/深色正文、边框、状态气泡可读，装饰不遮挡输入与告警 | 通过；地图装饰无事件权威，气泡和告警位于角色层上方 |

## P3 后续润色

- 动态日历、任务板和问号板仍保持原办公室纸张语义；可在未来仅替换材质，不改变点击入口。
- 角色为兼容既有运行时保持 18×32 原始帧，细节密度不会等同角色概念板的大幅展示，这是有意的低风险取舍。
- 右侧浅色 Command Center 沿用用户明暗偏好，不由深色星港主题强制改为深色。

final result: passed
